fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary]==3.2.3
sentence-transformers==3.3.1
# BGE-M3's pinned official snapshot contains PyTorch weights rather than a
# safetensors artifact.  This compatible release supports loading that trusted,
# locally pinned snapshot with the Jamba runtime's Torch 2.5.1.
transformers==4.46.3
sentencepiece==0.2.0
