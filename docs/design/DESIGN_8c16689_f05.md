# Design Session — US-110 F-05 final routing and notifications

> Linked from [DESIGN.md](DESIGN.md). This file records the F-05 requirement
> analysis and remains a permanent linked record.

## User Story

### US-110: Final routing and audience-specific notifications

As a municipal case operator
I want an eligible case routed exactly once to an active authoritative unit,
with separately persisted user and target-unit notifications
So that the responsible unit receives actionable context without exposing
internal details to the applicant.

F-05 consumes stable F-02 department/unit/request-type IDs and F-03's current
completion state. Routing is deterministic from authoritative registry data;
Jamba cannot choose or replace a target unit. A notification-generation
failure cannot reverse a persisted successful routing decision.

The orchestrator automatically starts F-05 as a durable internal operation
after F-04 reaches `completed`, including both `draft_ready` and
`review_required` results. There is no client-owned F-05 start endpoint. A
PostgreSQL-backed recovery evaluation may select complete cases that were not
triggered or whose current F-04 state is `not_requested`; it routes these and
review-required cases to the active `Others`/`Uncategorized` department and
unit. All cases, routing records, and notification state remain queryable from
PostgreSQL through the authorized application read model for later UI review.

The classified active department/unit is the routing target. A privileged
administrator may maintain an authoritative routing registry or override in
the database/backend administration surface; its administration workflow is
out of scope and F-05 does not add a client-facing override. Notifications are
structured PostgreSQL records only. E-mail integration is a non-dispatching
placeholder; F-05 sends no external message.

The applicant notification contains only process-facing information. The
target-unit notification may contain the case/document information its
authorized audience needs to act, including applicable validated F-03 fields
and F-04 output, but it is projected through the recipient's existing case
access controls and contains no information outside that accessible case.

## Gherkin Acceptance Criteria

Feature: F-05 final routing and audience-specific notifications

  Scenario: An eligible case is routed exactly once to its active target unit
    Given a case has a valid hierarchical F-02 classification
    And its F-03 completion status is `complete`
    And the authoritative routing registry marks the target unit active
    When the approved F-05 routing operation runs for its current revision
    Then it persists one routing record with `routing_status: routed`
    And its target department/unit IDs agree with the authoritative routing registry
    And a replay cannot create a second logical routing decision

  Scenario: The orchestrator automatically routes terminal F-04 work and recovers untriggered cases
    Given F-04 completes for an eligible current case revision
    When the orchestrator consumes its durable completion event
    Then it creates or replays one F-05 routing operation without a client start request
    When a complete case has no F-04 generation or missed its trigger
    Then PostgreSQL recovery evaluation makes it visible for review and routes it once to active `Others`/`Uncategorized`

  Scenario: Review-required correspondence is routed to the fallback category
    Given F-04 completed the current revision with `result_status: review_required`
    When the orchestrator starts F-05
    Then it routes the case to active `Others`/`Uncategorized` department and unit

  Scenario: User and target-unit notifications remain separate records
    Given a case was successfully routed
    When notification generation runs
    Then it persists an applicant-facing process notification without internal metadata
    And it persists a target-unit notification containing the approved operational context
    And either notification record can be observed independently of routing state
    And no external e-mail or other delivery is performed

  Scenario: Ineligible or inactive routing targets are rejected without a route
    Given a case is `needs_review`, has F-03 missing/invalid information, or has an inactive target unit
    When the approved F-05 routing operation is requested
    Then it rejects the operation with a machine-readable error
    And it creates no routing record or notification-generation job

  Scenario: A notification failure preserves a successful route
    Given a routing record is `routed`
    And Jamba notification generation has a transient failure
    When the notification attempt fails
    Then the routing record remains `routed`
    And the notification has a retryable failed state without publishing partial text

## Open Questions

| ID | Question | Raised By | Status | Resolution |
|---|---|---|---|---|
| OQ-139 | F-05'in public başlatma/okuma sınırı nedir: case-level `POST`/`GET` endpointleri mi, F-04 tamamlanınca orchestrator'ın otomatik çağrısı mı, yoksa ikisi mi? Authorization, Idempotency-Key, If-Match ve replay scope'u hangi kesin kuralları izler? | requirement-analysis | Resolved | Orchestrator F-04 completion event'i sonrası otomatik ve durable olarak başlatır; client-owned F-05 start endpoint yoktur. PostgreSQL source-of-truth/read model, yetkili UI'nın tetiklenmeyen veya geçmiş case/routing durumlarını görüntülemesini sağlar. |
| OQ-140 | F-05 eligibility F-04 ile nasıl bağlanır: yalnız `completed + draft_ready` mi route edilir; `review_required` no-source taslağı route edilebilir mi; F-04 hiç istenmemiş bir complete case için davranış nedir? | requirement-analysis | Resolved | `draft_ready` normal classified target'a route edilir. `review_required` ve current F-04 `not_requested` complete case'leri route edilir; active `Others`/`Uncategorized` department/unit fallback'ına gider. Orchestrator, PostgreSQL recovery evaluation ile missed/untriggered case'leri de değerlendirir. |
| OQ-141 | Notification teslim kapsamı nedir: F-05 yalnız PostgreSQL'de structured taslak/notification state mi üretir, yoksa e-posta/SMS/kurum içi kanal gibi gerçek bir dispatcher içerir mi? Varsa hangi kanal(lar), teslim idempotency'si ve retry terminal koşulu nedir? | requirement-analysis | Resolved | F-05 yalnız structured PostgreSQL notification state/taslağı üretir. E-mail integration placeholder olarak tutulur; dış dispatcher, kanal teslimi ve delivery workflow'u out of scope'tur. |
| OQ-142 | Versioned authoritative routing registry'nin sahibi ve kuralı nedir: F-02'nin classified unit'i doğrudan hedef mi, request type'a göre ayrı active-unit override/queue kuralı var mı, ve registry değiştiğinde mevcut route tekrar hesaplanır mı? | requirement-analysis | Resolved | Varsayılan hedef classified active department/unit'tir. Registry override'ları, SQL/backend erişimli ayrı bir admin işlemiyle yönetilebilir; F-05 bu admin workflow'unu veya client override API'sini içermez. |
| OQ-143 | Hedef birim notification context'inde hangi F-03 alanları, F-04 draft/citation bilgileri ve PII sınıfları bulunabilir; applicant notification için hangi alanlar kesinlikle dışarıda tutulur? | requirement-analysis | Resolved | Birim notification'ı, alıcının eriştiği case/doküman için gerekli F-03 ve F-04 bağlamını taşıyabilir. Applicant notification yalnız süreç bilgisi taşır; internal metadata ve erişim dışı case bilgisi sızdırılmaz. |
